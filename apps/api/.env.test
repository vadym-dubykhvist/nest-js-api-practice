# Committed to git on purpose: only test-only values.
# Never put real secrets here — use .env.local (gitignored) for those.
NODE_ENV=test
DB_HOST=localhost
DB_PORT=5433
DB_USERNAME=test
DB_PASSWORD=test
DB_NAME=nestjs_practice_test
DB_SYNCHRONIZE=true
JWT_SECRET=test-secret
