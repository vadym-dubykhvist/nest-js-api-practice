# Root env for docker-compose (variable substitution).
# Copy to .env and fill in. The API app's own runtime env lives in apps/api/.env.local.
POSTGRES_USER=nestjspractice
POSTGRES_PASSWORD=change_me
POSTGRES_DB=nestjspractice
JWT_SECRET=change_me
